function YourFunctionName() {
    make a variable and put the value of the HTML element whose id is nameInput into it
    if (variableName==null || variableNAME=="") {
        Bring_up_an_appropriate_alert_box
        return false;
    }
    Find the HTML element that has an Id of submitInfo and put text into it that includes the variable (user name)
}